# CRNP Audit + Seed Implementation

**Goal:** Seed Firebase with rich demo data, verify all auth flows, fix bugs, delete dead code.

## Done

1. **`seed.php`** — 16 products, 9 rent items, 4 users, 28 orders (7 days), 12 bookings, admin + cashier + kitchen accounts. Guarded behind `DEV_MODE`.
2. **🔧 Bug fix: `cashier/login.php:42`** — `$cashier['password']` → `password_hash` (seed stores `password_hash`, login was checking wrong field, making every cashier login fail).
3. **🔧 Bug fix: `kitchen/login.php:36`** — same field mismatch (`password` → `password_hash`).
4. **`admin/staff.php`** already uses `password_hash` correctly for both roles.
5. **Logged out as "verified"** when the project context shifted from `~/Projects/CRNP/` (legacy) to `~/Downloads/final/CRNP/` (2.0 refactor) earlier — both now synced via git pull/reset.

## Auth flow coverage (all paths verified)

| Path | Auth store | Login check | Status |
|------|-----------|-------------|--------|
| `/user` | `password_hash` | `password_hash` | ✅ |
| `/admins` | `password_hash` | `password_hash` | ✅ |
| `/cashiers` | `password_hash` | `password_hash` | ✅ fixed |
| `/kitchen` | `password_hash` | `password_hash` | ✅ fixed |

## No dead code found

Files: `config.php`, `db.php`, `firebaseRDB.php`, `init.php`, `mailer.php`, `seed.php`, `index.php`, 4× includes, 8× user/, 9× admin/, 4× cashier/, 4× kitchen/, 3× PHPMailer, 1× CSS, 1× JS. All referenced.

## Open

- PHPMailer classes are present but no mail server test was done (external dependency).
- OTP sending via SMTP needs real Gmail credentials to verify.
- Google OAuth requires `http://localhost:8000` in Authorized JavaScript origins (Cloud Console fix, not code).
- `/admin/signup.php` is auto-disabled after first admin — seed writes one, so signup form is locked. Good security, but user needs to know this.
